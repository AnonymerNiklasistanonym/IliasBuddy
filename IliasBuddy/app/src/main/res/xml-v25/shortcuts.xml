<?xml version="1.0" encoding="utf-8"?>
<shortcuts xmlns:android="http://schemas.android.com/apk/res/android">
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_current_entries"
        android:shortcutDisabledMessage="@string/shortcut_disabled"
        android:shortcutId="compose"
        android:shortcutLongLabel="@string/shortcut_long_label"
        android:shortcutShortLabel="@string/shortcut_short_label">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="com.niklasm.iliasbuddy.MainActivity"
            android:targetPackage="com.niklasm.iliasbuddy" />
        <!-- If your shortcut is associated with multiple intents, include them
             here. The last intent in the list determines what the user sees when
             they launch this shortcut. -->
    </shortcut>
    <!-- Specify more shortcuts here. -->
    <shortcut
        android:enabled="true"
        android:icon="@drawable/ic_settings_black"
        android:shortcutDisabledMessage="@string/shortcut_disabled2"
        android:shortcutId="compose2"
        android:shortcutLongLabel="@string/shortcut_long_label2"
        android:shortcutShortLabel="@string/shortcut_short_label2">
        <intent
            android:action="android.intent.action.VIEW"
            android:targetClass="com.niklasm.iliasbuddy.SettingsActivity"
            android:targetPackage="com.niklasm.iliasbuddy" />
    </shortcut>
</shortcuts>
